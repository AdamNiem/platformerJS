# platformerJS
A little platformer game made in html5 with vanilla javascript. Uses some basic delta-time calculations but might not be too reliable so running at 60fps is preferred.

To run:
Just drag the html file into a browser or type the file path of the html file in your browser
